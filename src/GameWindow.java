import javax.swing.*;

public class GameWindow extends JFrame {
    public GameWindow() {
        setTitle("Element Survival");
        setSize(800, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setResizable(true);
        setLocationRelativeTo(null);
    }

}